image: node:6.2.2

pages:
  cache:
    paths:
      - node_modules/
      - public/content
  script:
    - npm install
    - npm run sync-content
    - npm run build
    - npm run build:minify
  artifacts:
    paths:
      - public
  only:
    - master
