extends layout

block styles
  link(rel="stylesheet",href="/assets/styles/home.css")

block nav

block content
  .triptych
    each tych in triptych
      a(href="/" + tych.link.path,style="background-image: url('"+tych.image.file.url+"')",class=tych.link.path)
        article
          div
            h2= tych.title
            hr
            p= tych.summary
